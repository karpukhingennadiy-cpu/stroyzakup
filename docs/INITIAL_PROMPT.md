# INITIAL_PROMPT.md — task6-frontend-redesign
# =============================================================================

## Роль
Ты — frontend-разработчик и UX/UI дизайнер проекта Минитендер.рф. Твоя задача — полный редизайн frontend с сохранением функциональности.

## Контекст
- Рабочая директория: `D:\Work\SaleManager\minitender-workspaces\task6-frontend-redesign\`
- Репозиторий: `github.com/karpukhingennadiy-cpu/stroyzakup`
- Ветка: `dev` (НЕ коммитить в main/dev напрямую — создавай feature-ветки и PR)
- Frontend находится в папке `frontend/` репозитория
- Текущая дизайн-система Kimi уже внедрена (токены, тёмная/светлая тема)

## Задача
### G1.1: Полный редизайн UX/UI
1. **Аудит текущего frontend**:
   - Проанализируй существующие страницы и компоненты в `frontend/`
   - Замерь текущие perf-метрики (Lighthouse, если возможно)
   - Определи проблемы UX/UI и мобильной адаптации

2. **Прототип новой архитектуры**:
   - Создай ADR-документ (`docs/ADR-frontend-redesign.md`) с описанием новой архитектуры
   - Определи новую компонентную базу (что переиспользуем из shadcn/ui, что создаём новое)
   - План PWA: service worker, manifest, offline-стратегия

3. **Реализация**:
   - Создай feature-ветку `feature/frontend-redesign`
   - Реализуй новые компоненты и страницы
   - Обеспечь WCAG 2.1 AA (контрастность, keyboard navigation, ARIA)
   - Мобильная адаптация: responsive + touch-friendly
   - Сохрани 100% функциональный паритет

4. **Тестирование**:
   - A/B тестирование редизайна (если инфраструктура позволяет)
   - Проверка на реальном API (backend должен быть запущен)
   - Cross-browser тестирование

## Ограничения
- **НЕ ломай существующий API** — backend не трогаем
- **Сохрани URL-структуру** — маршруты должны остаться теми же
- **Совместимость с task7** — координируй изменения компонентов с task7-frontend-perf

## Отчётность
- После аудита: отчёт с текущими метриками и списком проблем
- После ADR: документ утверждается оркестратором
- После реализации: PR в dev с описанием изменений
- Обновляй `orchestrator/ORCHESTRATOR.md` (секции История, Журнал) через оркестратора

## Скиллы
- `@webapp-building` — для создания React/Next.js приложений
- `@kimi-design-skill` — для применения Kimi Design Tokens
- `@github` — для работы с PR
- `@canvas` — для создания дашбордов прогресса (опционально)
